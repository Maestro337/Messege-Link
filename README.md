# MessageLink (Java)

- Collects notifications via NotificationListenerService
- Stores them locally (Room)
- Displays unified list with search + app filter
- Optional HTTP POST to Raspberry Pi endpoint

## Enable notification access
Inside the app menu: "Enable notification access"

## Raspberry Pi
Settings -> enable -> set URL, e.g. http://192.168.1.10:8080/api/messages
